{
  "name": "yt-music-scrobbler",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "migrate": "dotenv -- pnpm --filter prisma-config run migrate",
    "generate": "dotenv -- pnpm --filter prisma-config run generate"
  },
  "devDependencies": {
    "turbo": "^2.0.4"
  },
  "packageManager": "pnpm@9.3.0",
  "dependencies": {
    "dotenv-cli": "^7.4.2"
  }
}
